<h2>{{name}}</h2>
<div>
    <h3>Capital:{{capital}}</h3>
    <p>Population:{{population}}</p>
    <p>Lenguages:</p>
    <ul>
        {{#each this.languages}}
        <li>{{name}}</li>
        {{/each}}
    </ul>
</div>
<img src="{{flag}}" alt="flag" width="320px" height="320px">